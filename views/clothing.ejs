<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Clothing</title>
    <style>
        /* General Styles */
  /* General Styles */
body {
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
    background: linear-gradient(to right, #00c6ff, #0072ff); /* Same gradient background */
    color: #333;
}

/* Remove underline for all links */
a {
    text-decoration: none; /* This will remove the underline */
}

/* Navbar Styles */
.navbar {
    background: #343a40;
    padding: 15px 30px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    position: sticky;
    top: 0;
    z-index: 100;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

.navbar-brand {
    font-weight: bold;
    color: #fff;
    font-size: 2em;
    letter-spacing: 1px;
}

.navbar-nav {
    display: flex;
    gap: 25px;
}

.navbar-nav .nav-link {
    color: #fff;
    padding: 10px 20px;
    border-radius: 5px;
    font-size: 1.1em;
    text-transform: uppercase;
    transition: all 0.3s ease;
}

.navbar-nav .nav-link:hover {
    background-color: #495057;
    transform: scale(1.1);
    color: #ffd700;
}

/* Container and Grid Styles */
.container {
    margin: 3rem auto;
    padding: 0 20px;
    max-width: 1200px;
    color: #fff;
}

h1 {
    text-align: center;
    font-size: 3em;
    margin-bottom: 40px;
    color: #fff;
    font-weight: bold;
    text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3);
}

.product-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 30px;
}

/* Product Card Styles */
.product-card {
    background-color: #fff;
    border-radius: 12px;
    overflow: hidden;
    text-align: center;
    box-shadow: 0 10px 15px rgba(0, 0, 0, 0.1);
    transform: scale(1);
    transition: all 0.3s ease;
    background: #f7f7f7;
}

.product-card:hover {
    transform: scale(1.05);
    box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
}

.product-card img {
    width: 100%;
    height: 250px;
    object-fit: cover;
    transition: transform 0.3s ease;
}

.product-card img:hover {
    transform: scale(1.1);
}

.product-card h3 {
    margin: 15px 0;
    font-size: 1.5em;
    color: #333;
    font-weight: bold;
}

.product-card p {
    margin: 0;
    padding: 0 15px 20px;
    font-size: 1em;
    color: #555;
}

.price {
    font-size: 1.2em;
    color: #ff6347;
    font-weight: bold;
    margin: 10px 0;
}

.product-card a {
    display: block;
    text-decoration: none;  /* Prevent underline */
}

.buy-now-btn {
    display: inline-block;
    margin: 10px 0 20px;
    padding: 12px 25px;
    background-color: #007bff;
    color: #fff;
    border-radius: 5px;
    font-weight: bold;
    transition: background-color 0.3s, transform 0.2s;
}

.buy-now-btn:hover {
    background-color: #0056b3;
    transform: translateY(-3px);
}

/* Footer Styles */
footer {
    background: #343a40;
    color: #fff;
    text-align: center;
    padding: 20px;
    margin-top: 50px;
}

footer a {
    font-weight: bold;
}

footer a:hover {
    text-decoration: underline;
}

.highlighted {
    background: linear-gradient(to right, #FFB6C1, #FFD700);  /* Soft gradient from light pink to gold */
    color: #333;               /* Dark text for contrast */
    padding: 8px 15px;         /* Adequate padding */
    border-radius: 5px;        /* Rounded corners */
    font-weight: bold;         /* Bold text */
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);  /* Subtle shadow for depth */
}
.product-card-link {
    text-decoration: none; /* Remove the default underline */
    display: block; /* Make the link block-level */
    color: inherit; /* Inherit the text color from the card */
}

.product-card-link:hover .product-card {
    transform: scale(1.05); /* Apply hover effect on the card */
    box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
}

.product-card {
    background-color: #fff;
    border-radius: 12px;
    overflow: hidden;
    text-align: center;
    box-shadow: 0 10px 15px rgba(0, 0, 0, 0.1);
    transform: scale(1);
    transition: all 0.3s ease;
    background: #f7f7f7;
}
.product-card-link {
    text-decoration: none; /* Remove underline */
    display: block; /* Make the link block-level */
    color: inherit; /* Inherit color for all content inside the card */
    position: relative; /* Required for child element positioning */
}

.product-card .button {
    display: inline-block;
    margin-top: 10px;
    padding: 10px 20px;
    background-color: #007bff;
    color: #fff;
    text-decoration: none;
    border-radius: 5px;
    font-weight: bold;
    transition: background-color 0.3s ease, transform 0.2s ease;
    cursor: pointer;
}

.product-card .button:hover {
    background-color: #0056b3;
    transform: translateY(-3px);
}

.product-card:hover .button {
    transform: none; /* Ensure the button doesn't conflict with card hover effects */
}
/* Ensure the link behaves properly */
.product-card-link {
    text-decoration: none; /* Remove underline */
    display: block; /* Make the entire card clickable */
    color: inherit; /* Inherit color for text inside */
}

.product-card-link:hover .product-card {
    transform: scale(1.05); /* Card hover effect */
    box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2); /* Card hover shadow */
}


    </style>
</head>
<body>
    <!-- Navbar -->
    <%- include('./partials/nav') %>

    <!-- Clothing Products -->
    <div class="container">
        <h1>Clothing</h1>
        <p class="highlighted">Discover the latest trends in fashion for all seasons.</p>
        <div class="product-grid">
            <% clothing.forEach(product => { %>
                <a href="/blog/product/clothing/<%= product.id %>" class="product-card-link">
                    <div class="product-card">
                        <img src="<%= product.image %>" alt="<%= product.name %>">
                        <h3><%= product.name %></h3>
                        <p><%= product.description %></p>
                        <div class="price"><%= product.currency %> <%= product.price.toFixed(2) %></div>
                        <span class="button">Shop Now</span>
                    </div>
                </a>
            <% }); %>
        </div>
    </div>

    <!-- Footer -->
    <footer>
        <p>&copy; 2025 Shopping Website | <a href="/privacy-policy">Privacy Policy</a></p>
    </footer>
</body>
</html>
