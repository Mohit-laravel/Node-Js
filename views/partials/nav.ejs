<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shopping Home</title>
    <style>
        /* General Styles */
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(to right, #00c6ff, #0072ff); /* Beautiful gradient background */
            color: #333;
        }

        /* Navbar Styles */
        .navbar {
            background: #343a40;
            padding: 15px 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            z-index: 100;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .navbar-brand {
            font-weight: bold;
            color: #fff;
            text-decoration: none;
            font-size: 2em;
            letter-spacing: 1px;
        }

        .navbar-nav {
            display: flex;
            gap: 25px;
        }

        .navbar-nav .nav-link {
            color: #fff;
            text-decoration: none;
            padding: 10px 20px;
            border-radius: 5px;
            font-size: 1.1em;
            text-transform: uppercase;
            transition: all 0.3s ease;
        }

        .navbar-nav .nav-link:hover {
            background-color: #495057;
            transform: scale(1.1);
            color: #ffd700;
        }

        /* Container and Grid Styles */
        .container {
            margin: 3rem auto;
            padding: 0 20px;
            max-width: 1200px;
            color: #fff;
        }

        h1 {
            text-align: center;
            font-size: 3em;
            margin-bottom: 40px;
            color: #fff;
            font-weight: bold;
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3);
        }

        .product-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 30px;
        }

        /* Product Card Styles */
        .product-card {
            background-color: #fff;
            border-radius: 12px;
            overflow: hidden;
            text-align: center;
            box-shadow: 0 10px 15px rgba(0, 0, 0, 0.1);
            transform: scale(1);
            transition: all 0.3s ease;
            background: #f7f7f7;
        }

        .product-card:hover {
            transform: scale(1.05);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
        }

        .product-card img {
            width: 100%;
            height: 250px;
            object-fit: cover;
            transition: transform 0.3s ease;
        }

        .product-card img:hover {
            transform: scale(1.1);
        }

        .product-card h3 {
            margin: 15px 0;
            font-size: 1.5em;
            color: #333;
            font-weight: bold;
        }

        .product-card p {
            margin: 0;
            padding: 0 15px 20px;
            font-size: 1em;
            color: #555;
        }

        .product-card a {
            display: inline-block;
            margin: 10px 0 20px;
            padding: 12px 25px;
            background-color: #007bff;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            transition: background-color 0.3s, transform 0.2s;
        }

        .product-card a:hover {
            background-color: #0056b3;
            transform: translateY(-3px);
        }

        /* Footer Styles */
        footer {
            background: #343a40;
            color: #fff;
            text-align: center;
            padding: 20px;
            margin-top: 50px;
        }

        footer a {
            color: #fff;
            text-decoration: none;
            font-weight: bold;
        }

        footer a:hover {
            text-decoration: underline;
        }

        .user-profile {
    position: relative;
    display: inline-block;
}

.user-button {
    background-color: #495057;
    color: #fff;
    border: none;
    padding: 10px 20px;
    font-size: 1.1em;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s;
}

.user-button:hover {
    background-color: #343a40;
}

.dropdown-menu {
    display: none;
    position: absolute;
    right: 0;
    background-color: #ffffff;
    min-width: 150px;
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
    border-radius: 5px;
    overflow: hidden;
    z-index: 1;
}

.dropdown-item {
    color: #333;
    padding: 10px 15px;
    text-decoration: none;
    display: block;
    transition: background-color 0.3s;
}

.dropdown-item:hover {
    background-color: #f1f1f1;
}

.user-profile:hover .dropdown-menu {
    display: block;
}
.navbar {
    background: #343a40;
    padding: 15px 30px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    position: sticky;
    top: 0;
    z-index: 100;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

.navbar-nav {
    display: flex;
    gap: 25px;
    align-items: center; /* Align all items vertically in the center */
}

.navbar-nav .nav-link {
    color: #fff;
    text-decoration: none;
    padding: 10px 20px;
    border-radius: 5px;
    font-size: 1.1em;
    text-transform: uppercase;
    transition: all 0.3s ease;
}

.navbar-nav .nav-link:hover {
    background-color: #495057;
    transform: scale(1.1);
    color: #ffd700;
}

/* User Profile Button Alignment */
.user-profile {
    position: relative;
    display: inline-block;
    margin-left: auto; /* Push it to the right, if needed */
}

.user-button {
    background-color: #495057;
    color: #fff;
    border: none;
    padding: 10px 20px; /* Same padding as other nav items */
    font-size: 1.1em;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s;
    text-transform: uppercase; /* Match text style */
}

.user-button:hover {
    background-color: #343a40;
}

.dropdown-menu {
    display: none;
    position: absolute;
    right: 0;
    background-color: #ffffff;
    min-width: 150px;
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
    border-radius: 5px;
    overflow: hidden;
    z-index: 1;
}

.dropdown-item {
    color: #333;
    padding: 10px 15px;
    text-decoration: none;
    display: block;
    transition: background-color 0.3s;
}

.dropdown-item:hover {
    background-color: #f1f1f1;
}

.user-profile:hover .dropdown-menu {
    display: block;
}

/* Adjust profile image styles */
.user-profile {
    position: relative;
    display: flex;
    align-items: center;
}

.user-image {
    width: 35px; /* Adjust size to fit the navbar */
    height: 35px;
    border-radius: 50%;
    cursor: pointer;
    object-fit: cover;
    border: 2px solid #fff;
    transition: transform 0.3s;
}

.user-image:hover {
    transform: scale(1.1); /* Slight zoom effect */
}

/* Align the dropdown menu with the profile image */
.dropdown-menu {
    display: none;
    position: absolute;
    top: 100%; /* Place the dropdown below the image */
    right: 0;
    background-color: #ffffff;
    min-width: 150px;
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
    border-radius: 5px;
    overflow: hidden;
    z-index: 1;
}

.dropdown-item {
    color: #333;
    padding: 10px 15px;
    text-decoration: none;
    display: block;
    transition: background-color 0.3s;
}

.dropdown-item:hover {
    background-color: #f1f1f1;
}

/* Show the dropdown menu on hover */
.user-profile:hover .dropdown-menu {
    display: block;
}

/* Adjust the navbar links and profile alignment */
.navbar-nav {
    display: flex;
    align-items: center; /* Ensure all items are aligned properly */
    gap: 20px; /* Add spacing between items */
}
</style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar">
        <a class="navbar-brand" href="#">SHOPPING</a>
        <ul class="navbar-nav">
            <li><a class="nav-link" href="/">Home</a></li>
            <li><a class="nav-link" href="/blog/categories/accessories">Accessories</a></li>
            <li><a class="nav-link" href="/blog/categories/electronics">Electronics</a></li>
            <li><a class="nav-link" href="/blog/categories/clothing">Clothing</a></li>
            <li><a class="nav-link" href="/blog/categories/home-appliances">Home Appliances</a></li>
            <% if (user) { %>
                <!-- If user is logged in, show profile image -->
                <li class="user-profile">
                    <img 
                        src="<%= user.profileImageUrl || '/default-profile.png' %>" 
                        alt="User Profile" 
                        class="user-image"
                    />
                    <div class="dropdown-menu">
                        <a href="/user/profile" class="dropdown-item">My Profile</a>
                        <a href="/user/signout" class="dropdown-item">Logout</a>
                    </div>
                </li>
                <% } else { %>
                <!-- If user is not logged in, show Login/Register options -->
                <li class="user-profile">
                    <a href="/user/signin" class="nav-link">Login</a>
                </li>
                <li class="user-profile">
                    <a href="/user/signup" class="nav-link">Register</a>
                </li>
                <% } %>
        </ul>
    </nav>
</body>
</html>
